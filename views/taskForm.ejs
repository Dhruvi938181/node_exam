<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>Edit Task</h2>

<h2>Edit Task</h2>

<form action="/task/update/<%= data._id %>" method="post">
  
  <input type="text" name="title" value="<%= data.title %>">

  <select name="categoryId">
    <% categories.forEach(c => { %>
      <option value="<%= c._id %>"
        <%= c._id.toString() === data.categoryId.toString() ? "selected" : "" %>>
        <%= c.title %>
      </option>
    <% }) %>
  </select>

  <button>Update</button>
</form>


</body>
</html>